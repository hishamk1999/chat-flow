import { ChatWelcomeSection } from '@/features/chat/components/ChatWelcomeSection';

/**
 * Route-level page: compose features only. No API calls or business rules.
 */
export function ChatPage() {
  return <ChatWelcomeSection />;
}
